import React from 'react'
import Header from '../../components/Header/Header';

export default function ResumeCreator() {
    return (
        <div className='flex max-h-screen h-screen flex-col items-center justify-start  bg-gradient-to-b from-black to-[#0D0D0D]'>
            <Header />
            <span>ResumeCreator</span>
        </div>
    );
}